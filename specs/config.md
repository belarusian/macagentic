# Configuration

`config/config.toml` provides project defaults.
`~/.config/macagentic/config.toml` overrides those defaults.

Supported keys are `model`, `openai_api_key`, and `custom_prompt`.
Command-line options override TOML values.
